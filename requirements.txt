# Telegram Network Nurturing Agent - Dependencies

# Core Framework
python-telegram-bot>=20.7
crewai>=0.28.0
crewai-tools>=0.4.0

# API Integrations
pyairtable>=2.2.0
openai>=1.6.0
google-generativeai>=0.3.0

# Note: There's a known protobuf version conflict between crewai (opentelemetry-proto>=5.0)
# and google-generativeai (google-api-core<4.0). pip check will show warnings but the
# application runs correctly. Do not upgrade all packages simultaneously.

# Web Search
tavily-python>=0.3.0

# Data Processing
pandas>=2.1.0
openpyxl>=3.1.0
Pillow>=10.1.0
pydub>=0.25.1
regex>=2023.10.3

# Utilities
python-dotenv>=1.0.0
pydantic>=2.5.0
tabulate>=0.9.0

# Audio Processing
aiohttp>=3.9.0
aiofiles>=23.2.1

# Async Support
nest-asyncio>=1.5.8

# LinkedIn Scraper (optional - only needed if LINKEDIN_SCRAPER_ENABLED=true)
selenium>=4.16.0
undetected-chromedriver>=3.5.4
beautifulsoup4>=4.12.0
lxml>=4.9.0
